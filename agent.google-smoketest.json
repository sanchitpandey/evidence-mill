{
  "adapter": "google",
  "model": "google/gemini-2.5-flash",
  "project_env": "GOOGLE_CLOUD_PROJECT",
  "max_tokens": 1024,
  "temperature": 0.1,
  "max_turns": 4,
  "runs": 1,
  "seed_list": [0],
  "note": "Smoke-test config only, mirroring jobcrawler's providers.py VertexAIProvider (OAuth via google.auth) but using google/gemini-2.5-flash, NOT jobcrawler's google/gemini-2.5-flash-lite: manual probing found flash-lite reliably emits malformed_function_call (finish_reason) through the Vertex AI OpenAI-compatible endpoint when the first user message is raw JSON credentials, exactly the format evaluation/calibrate.py's harness sends -- flash does not have this problem (3/3 clean tool calls in isolated testing). NOT the frozen submission config -- see agent.json for that. Kept deliberately tiny (1 run, 4 turns) to validate the Google adapter path end to end without burning real cohort budget."
}
