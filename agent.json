{
  "adapter": "openai",
  "model": "gpt-5.4-mini",
  "api_key_env": "OPENAI_API_KEY",
  "token_param": "max_completion_tokens",
  "max_tokens": 8192,
  "max_turns": 16,
  "runs": 16,
  "seed_base": 20260913,
  "note": "Frozen configuration for the canonical 16-rollout calibration. seed_base pins BOTH halves of rollout i: the environment is reproduced from episode_seed = seed_base + i (run id, credentials, object ids and flag, derived through the secret runtime episode key), and the model samples at the same seed. Same seed therefore gives the same environment guaranteed, and the same trajectory only likely: OpenAI documents `seed` as Beta and best-effort, and temperature is fixed at 1.0 on GPT-5-series reasoning models. Check the report's `seeded`, `reproducible_episodes` and `system_fingerprints` fields. The RECORDED 12/16 cohort predates seeding and is not replayable; see reports/."
}
